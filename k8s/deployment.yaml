apiVersion: apps/v1
kind: Deployment
metadata:
  name: devsecops-app
  labels:
    app: devsecops-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devsecops-app
  template:
    metadata:
      labels:
        app: devsecops-app
    spec:
      containers:
      - name: app
        image: DOCKER_USERNAME_PLACEHOLDER/devsecops-app:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
        # --- Azure Database Environment Variables ---
        - name: DB_HOST
          value: "DB_HOST_PLACEHOLDER"
        - name: DB_USER
          value: "DB_USER_PLACEHOLDER"
        - name: DB_NAME
          value: "postgres"
        # --- SECRETS FETCHED FROM AZURE KEY VAULT VIA K8S SECRETS ---
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: devsecops-secrets
              key: db-password
        # --- AWS S3 Environment Variables ---
        - name: AWS_REGION
          value: "ap-south-1"  
        - name: AWS_S3_BUCKET
          value: "AWS_S3_BUCKET_PLACEHOLDER"
        - name: AWS_ACCESS_KEY_ID
          value: "AWS_ACCESS_KEY_ID_PLACEHOLDER"
        # --- SECRETS FETCHED FROM AZURE KEY VAULT VIA K8S SECRETS ---
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: devsecops-secrets
              key: aws-secret-key
        # --- Health Checks ---
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: devsecops-service
spec:
  type: NodePort
  selector:
    app: devsecops-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
